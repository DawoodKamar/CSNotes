import styles from "./quiz.module.css";

// Quiz component, not yet implemented
const Quiz = () => {
  return (
    <div className={styles.container}>
      <h2>Quiz Time</h2>
    </div>
  );
};

export default Quiz;
